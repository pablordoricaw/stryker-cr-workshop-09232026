# Additional packages the provided app needs beyond the Apps runtime.
# FastAPI + uvicorn are pre-installed in the Databricks Apps runtime.
# psycopg (Postgres driver) and the Databricks SDK are NOT guaranteed pre-installed,
# so pin them here — a missing psycopg is the #1 cause of Lakebase app crashes.
databricks-sdk>=0.81.0
psycopg[binary]>=3.1
