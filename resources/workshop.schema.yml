# The workshop schema, within the workshop catalog.
resources:
  schemas:
    workshop_schema:
      catalog_name: ${var.catalog}
      name: ${var.schema}
      comment: "Workshop domain schema (bronze/silver/gold tables and metric views land here)."
